{
  "name": "daikin-ir",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "del-cli dist",
    "lint": "tslint --format stylish --project .",
    "postinstall": "postinstall-build --silent dist",
    "sample-build": "npm run sample-clean & tsc --project sample",
    "sample-clean": "del-cli sample/dist",
    "sample-lint": "tslint --format stylish --project sample",
    "sample": "npm run sample-build & node sample/dist/sample/sample.js"
  },
  "author": "Mossuru777",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "tslint": "^5.9.1",
    "tslint-config-airbnb": "^5.8.0"
  },
  "dependencies": {
    "@types/node": "^8.10.1",
    "@types/sprintf-js": "^1.1.0",
    "del-cli": "^1.1.0",
    "postinstall-build": "^5.0.1",
    "typescript": "^2.8.1"
  }
}
